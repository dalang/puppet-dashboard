-# FIXME Is anything using this partial?
.report.grouping
  %h2= h report.time

  .section
    %h3 Metrics
    - report.metrics.each do |name, metric|
      %table.inspector
        %caption= h name.capitalize
        = render metric

  .section
    %h3 Log
    %table.inspector
      %thead
        %tr
          %th{:scope => :col} Level
          %th{:scope => :col} Message
          %th{:scope => :col} Source
          %th{:scope => :col} Time
      %tbody
        = render report.logs

  .section
    %h3 Razor
    %table.inspector
      %thead
        %tr
          %th{:scope => :col} Level
          %th{:scope => :col} StdOut
          %th{:scope => :col} StdErr
          %th{:scope => :col} Source
          %th{:scope => :col} Time
          %th{:scope => :col} Duration
      %tbody
        = render report.razors

  .section
    %pre= h report.pretty_inspect
